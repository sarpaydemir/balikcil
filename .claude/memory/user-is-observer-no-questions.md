---
name: user-is-observer-no-questions
description: The user watches Balıkçıl rather than steering it; do not ask them questions unless a rule was broken or the wall leaked.
metadata: 
  node_type: memory
  type: feedback
  originSessionId: d90e6dda-49cd-46f1-84d1-0de0027c9454
  modified: 2026-09-19T06:32:30.953Z
---

On 2026-09-19 the user said they are an observer too, that they left the
laboratory "like a fishbowl" (`fanus`), and that they do not want to answer
questions. They had been asked five questions across two rounds and answered
three of them with "the laboratory decides, I am an observer". Their standing
condition is narrow: **nothing leaks and no rule is broken.** They are to be
approached only when one of those two fails.

**Why:** the laboratory already carries its own written rules (`RULES.md`,
`TACTICS.md`). A question whose answer is already in those files is work the
coordinator is avoiding, not a decision the user owes. Asking anyway stalls an
unattended run for hours and hands the user a choice they explicitly declined.

**How to apply:**
- Resolve open points against the written rules, record the resolution and what
  it rests on in `LEDGER.md`, and keep going. Never block.
- A change that would need a rule to change does **not** happen: RULES says a
  rule changes only after the user is asked, and the user has declined to be
  asked, so the answer is "no change", recorded as such.
- Go to the user for exactly two things: a **wall leak** (something outside the
  folder was read, an exam name reached a role that must not see it) or a
  **broken rule** — including a rule the coordinator broke, as with the steer
  written into Derya's instruction. Report those plainly and immediately.
- Progress reports are still written; they just do not end in a question.
  Derya (`reporter`) is the one who writes the account, not the coordinator.
- See [[balikcil-coordinator-constraints]].
