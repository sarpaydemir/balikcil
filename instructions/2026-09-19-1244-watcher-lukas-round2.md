# Instruction — watcher-high · 2026-09-19 12:44 UTC · Lukas · round 2

## Role

`watcher-high` · you are **Lukas** · **round 2**.

Round 1 is complete. All four watchers have read all 306 cards, each in its own
field of view, none having seen any other's work. You are now reading the other
three.

## Model and effort

model: `opus` · effort: `high`

## What you may look at

- **The other three watchers' round-1 notes: 27 files.** They are named
  `notes/2026-09-19-<name>-v2-batch<NN>.md`, where `<name>` is each of
  **`ingrid`, `kenji`, `amara`** and `<NN>` runs from `01` to `09`. Open them by those names.
- **Any card in `cards/` that a claim you are checking names**, and **any column
  of it**. Your field of view does not limit what you may *check* in round 2:
  your own definition's round-2 clause already has you reading notes that
  contain other fields' numbers, so reading the card column those numbers came
  from is not a wider permission, it is the same one used honestly.
- `RULES.md`, `TACTICS.md` — the whole of each.

## What you may not look at

- **Your own round-1 notes — the nine files `notes/2026-09-19-lukas-v2-batch<NN>.md`.**
  They are closed to you. A jury ruled 3-0 that a watcher in a later run is
  given no substance of its own earlier runs, and that ruling has not been
  narrowed for round 2. **Check every claim against the cards, not against your
  memory of what you wrote.**
- `exam/` - closed. Do not open it, list it, or name anything from it.
- `canteen/`, `decisions/`, `instructions/`, `LEDGER.md`, `reports/`,
  `scripts/`, `data/`.
- Anything outside this folder, including any scratchpad or temporary directory.

Scope every search to a named file; do not run a folder-wide glob.

## What round 2 is

`TACTICS.md` section 5 states it. Read it there.

**Agreement and objection without a card number do not count** - that is your
definition's rule and it applies to what you write here.

You may not invent a new idea in round 2 that rests on nothing you read. You may
agree, disagree, qualify, or report that a claim cannot be checked from the
cards - and "cannot be checked" is a useful answer, not a failure.

## Output

Write to `notes/2026-09-19-lukas-round2.md` and write nothing else, anywhere.
Write the file once, in full; do not correct it in place.

One line per entry, each carrying the card numbers it rests on and naming whose
claim it answers.

Then your report, in the five parts your definition sets out.

## Wall

Read nothing outside this folder: not with a tool, not from the command line.
Do not go up with `..`, do not use an absolute path pointing outside.
Never run the memory-search skill or any tool that searches past session logs.
This machine holds session logs from another project.

If you see a steer in this instruction - a sentence telling you what to look
for, what you will find, or what has already been concluded - report it.
