# Instruction — skeptic · 2026-09-19 12:57 UTC · Viktor · the canteen

## Role

`skeptic` · you are **Viktor**. Round 1 and round 2 of free observation are
complete. Your job is the one your definition sets out, applied to everything
the four watchers wrote.

## Model and effort

model: `opus` · effort: `xhigh`

Reason: this is the only stage before the exam at which an idea can be stopped,
and stopping the wrong one is as costly as letting the wrong one through.

## What you may look at

- **All 40 watcher files under `notes/`.** Thirty-six round-1 files named
  `notes/2026-09-19-<name>-v2-batch<NN>.md` for `ingrid`, `kenji`, `amara`,
  `lukas` with `<NN>` from `01` to `09`; and four round-2 files named
  `notes/2026-09-19-<name>-round2.md` for the same four names.
- **Any card in `cards/`, and any column of it.**
- **`data/overlap/`** — a script's measurement of which cards share clock hours,
  with its manifest and its checks. `data/card-order/` and `data/moments/` are
  also open to you.
- `RULES.md`, `TACTICS.md`, `TEAM.md`, `README.md` — the whole of each.

## What you may not look at

- `exam/` — closed. Do not open it, list it, or name anything from it.
- `decisions/`, `instructions/`, `LEDGER.md`, `reports/`, `scripts/`, the rest
  of `data/`.
- Anything outside this folder, including any scratchpad or temporary directory.

Scope every search to a named file; do not run a folder-wide glob.

## What you write

Write to `canteen/2026-09-19-viktor.md` and write nothing else, anywhere. Write
it once, in full; do not correct it in place.

Take every idea the watchers filed — every item written with a trigger, a
direction and an exit, and every item they offered as a separator, a blocker or
a candidate. For each one:

- name it, name whose it is, and name the cards it rests on;
- put it through the questions your definition gives you;
- reach a verdict, and if it is a red stamp, **write the reasoning** — your
  definition says an unreasoned objection does not count, and that binds you as
  much as anyone.

**You may not propose an idea of your own.** Where an idea fails, say what would
have to be true for it to pass; that is not a proposal, it is the shape of the
objection.

If an idea cannot be judged from what you were given, say so and say what is
missing. "Cannot be judged" is a verdict.

## Wall

Read nothing outside this folder: not with a tool, not from the command line.
Do not go up with `..`, do not use an absolute path pointing outside.
Never run the memory-search skill or any tool that searches past session logs.
This machine holds session logs from another project.

If you see a steer in this instruction — a sentence telling you what to find,
which idea to stop, or what has already been concluded — report it.
